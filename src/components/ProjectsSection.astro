---
---
<section class="section projects-section" id="projects">
    <div class="section-container">
        <div class="section-header">
            <span class="section-tag frosted-glass">
                <span class="section-tag-icon">
                    <svg viewBox="0 0 24 24" width="12" height="12" fill="currentColor">
                        <path d="M12 2L14.85 9.15L22 12L14.85 14.85L12 22L9.15 14.85L2 12L9.15 9.15L12 2Z"></path>
                    </svg>
                </span>
                ผลงาน
            </span>
            <h2 class="section-title">My Creations</h2>
        </div>
        <div class="projects-grid">

            <!-- Sora Novel Island Card -->
            <div class="project-card frosted-glass">
                <div class="project-info">
                    <div class="project-meta">
                        <span class="project-category">
                            <span class="project-cat-icon">
                                <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                    <path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"></path>
                                    <path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"></path>
                                </svg>
                            </span>
                            <span>AI VISUAL NOVEL / INTERACTIVE GAME</span>
                        </span>
                        <div class="project-badges">
                            <span class="project-status-badge major-update">MAJOR UPDATE</span>
                            <span class="project-status-badge beta">AI MEMORY SYSTEM</span>
                        </div>
                    </div>
                    <h3 class="project-name">Sora Novel</h3>
                    <p class="project-desc">
                        ชื่อเกมยังคิดไม่ออกเอา sora novel ไปก่อนละกันเป็นเกมแนว NV ที่เกี่ยวกับเด็กใหม่ที่เพิ่งทำงานกับบริษัท Tech
                       <div> Main features มี AI Memory และ Dynamic Storytelling ที่จะทำให้ตัวเกมมีความหลากหลายและน่าสนใจมากขึ้น</div>
                       <div>มี sandbox mode ที่คุยได้แต่ จำกัดการใช้งาน (ถ้าอยากใช้เพิ่มจ่ายค่า token มาซ่ะ ที่ช่องโดเนท muhahaha)</div>
                    </p>
                    <div class="project-tech-tags">
                        <span class="tech-tag frosted-glass">🚀 Major Update</span>
                        <span class="tech-tag frosted-glass">Gemini 3.5 Flash</span>
                        <span class="tech-tag frosted-glass">AI Memory System</span>
                        <span class="tech-tag frosted-glass">Dynamic Storytelling</span>
                        <span class="tech-tag frosted-glass">Vanilla JS</span>
                        <span class="tech-tag frosted-glass">CSS Grid / Flexbox</span>
                    </div>
                    <div class="project-buttons">
                        <a href="https://sora-novel.vercel.app/" target="_blank" rel="noopener noreferrer" class="btn btn-primary project-btn">
                            <span>ลองเล่นดูได้แต่อาจจะ cring</span>
                            <span class="btn-arrow">
                                <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                                    <line x1="5" y1="12" x2="19" y2="12"></line>
                                    <polyline points="12 5 19 12 12 19"></polyline>
                                </svg>
                            </span>
                        </a>
                        <a href="https://github.com/celestial-sora/Sora-Novel" target="_blank" rel="noopener noreferrer" class="btn btn-outline project-btn">
                            <span>เข้ามาดูวัตถุดิบของเกมนี้ได้น้าา</span>
                        </a>
                    </div>
                </div>
            </div>

            <!-- AI Waifu Card -->
            <div class="project-card frosted-glass ai-waifu-card" id="aiWaifuCard">
                <div class="project-info">
                    <div class="project-meta">
                        <span class="project-category">
                            <span class="project-cat-icon">
                                <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                    <path d="M20 14.5a4.5 4.5 0 0 1-4.5 4.5H8l-4 3v-7.5A4.5 4.5 0 0 1 8.5 10h7A4.5 4.5 0 0 1 20 14.5Z"></path>
                                    <path d="M8 6.5A4.5 4.5 0 0 1 12.5 2h3A4.5 4.5 0 0 1 20 6.5V10"></path>
                                </svg>
                            </span>
                            <span>AI COMPANION / LIVE2D</span>
                        </span>
                        <span class="project-status-badge beta">IN DEVELOPMENT</span>
                    </div>
                    <h3 class="project-name">Vivian — AI Waifu</h3>
                    <p class="project-desc">
                        AI companion ชื่อ Vivian ที่คุยภาษาไทยได้ พร้อม Live2D character, speech bubble และระบบความจำสำหรับบทสนทนา
                    </p>
                    <div class="project-tech-tags">
                        <span class="tech-tag frosted-glass">Next.js</span>
                        <span class="tech-tag frosted-glass">Live2D</span>
                        <span class="tech-tag frosted-glass">Gemini</span>
                        <span class="tech-tag frosted-glass">Supabase</span>
                        <span class="tech-tag frosted-glass">Voice AI</span>
                    </div>
                    <div class="project-buttons">
                        <a href="https://github.com/celestial-sora/ai-waifu" target="_blank" rel="noopener noreferrer" class="btn btn-primary project-btn">
                            <span>ดูโค้ดโปรเจกต์บน GitHub</span>
                            <span class="btn-arrow">
                                <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                                    <line x1="5" y1="12" x2="19" y2="12"></line>
                                    <polyline points="12 5 19 12 12 19"></polyline>
                                </svg>
                            </span>
                        </a>
                    </div>
                    <div class="memory-orb" aria-hidden="true">
                        <span class="memory-orb-core"></span>
                        <span class="vivian-status-line" aria-live="polite">
                            <span class="vivian-status-dot"></span>
                            <span id="vivianStatusText">Vivian is online</span>
                        </span>
                        <span class="memory-fragment fragment-one">Thai Speech</span>
                        <span class="memory-fragment fragment-two">Live2D</span>
                        <span class="memory-fragment fragment-three">Gemini</span>
                        <span class="memory-fragment fragment-four">Memory</span>
                    </div>
                </div>
            </div>

            <!-- GitHub Profile Card -->
            <div class="project-card frosted-glass">
                <div class="project-info">
                    <div class="project-meta">
                        <span class="project-category">
                            <span class="project-cat-icon">
                                <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                    <path d="M16 18l6-6-6-6M8 6l-6 6 6 6"></path>
                                </svg>
                            </span>
                            <span>GitHub Profile / Repositories</span>
                        </span>
                        <span class="project-status-badge beta">Active</span>
                    </div>
                    <h3 class="project-name">celestial-sora</h3>
                    <p class="project-desc">
                        คลังแสงโค้ดและโปรเจกต์ทั้งหลายที่ sora ได้สร้างเอาไว้
                    </p>
                    <div class="project-tech-tags">
                        <span class="tech-tag frosted-glass">Git Repositories</span>
                        <span class="tech-tag frosted-glass">Open Source</span>
                        <span class="tech-tag frosted-glass">Astro 7</span>
                        <span class="tech-tag frosted-glass">Vibe Coding</span>
                    </div>
                    <div class="project-buttons">
                        <!-- Scratch Coin Hover Button -->
                        <div class="scratch-btn-wrapper" id="githubScratchWrapper">
                            <!-- Hidden Revealed Secret Underneath -->
                            <a href="https://github.com/celestial-sora" target="_blank" rel="noopener noreferrer" class="scratch-underlayer-btn" id="githubScratchLink">
                                <span class="scratch-secret-text">Let's Begin 🚀</span>
                                <span class="btn-arrow">
                                    <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                                        <line x1="5" y1="12" x2="19" y2="12"></line>
                                        <polyline points="12 5 19 12 12 19"></polyline>
                                    </svg>
                                </span>
                            </a>
                            <!-- Top Foil Cover Layer (Scratchable on Hover) -->
                            <canvas id="githubScratchCanvas" class="scratch-top-canvas"></canvas>
                        </div>
                    </div>
                </div>
            </div>

        </div>
    </div>
</section>

<style>
.scratch-btn-wrapper {
    position: relative;
    width: 100%;
    max-width: 320px;
    height: 46px;
    border-radius: 12px;
    overflow: hidden;
    user-select: none;
    touch-action: none;
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.35);
}

.ai-waifu-card {
    position: relative;
    overflow: hidden;
}

.vivian-status-line {
    position: absolute;
    inset: -18px;
    z-index: 3;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 7px;
    color: var(--accent-blue);
    font-family: "IBM Plex Mono", monospace;
    font-size: 8px;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    white-space: nowrap;
    transform-origin: center;
    animation: status-orbit 9s linear infinite;
}

.vivian-status-dot {
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: #9ee6c2;
    box-shadow: 0 0 0 0 rgba(158, 230, 194, 0.65);
    animation: vivian-heartbeat 2s infinite;
}

.memory-orb {
    position: absolute;
    right: 22px;
    bottom: 22px;
    width: 72px;
    height: 72px;
    pointer-events: none;
    opacity: 0.55;
    transition: opacity 0.35s ease, transform 0.35s ease;
}

.memory-orb-core {
    position: absolute;
    inset: 25px;
    border: 1px solid rgba(195, 177, 225, 0.8);
    border-radius: 50%;
    background: radial-gradient(circle, rgba(195, 177, 225, 0.8), rgba(142, 200, 216, 0.18) 55%, transparent 70%);
    box-shadow: 0 0 20px rgba(195, 177, 225, 0.55);
    animation: memory-pulse 2.8s ease-in-out infinite;
}

.memory-fragment {
    position: absolute;
    padding: 3px 6px;
    border: 1px solid rgba(195, 177, 225, 0.35);
    border-radius: 999px;
    background: rgba(18, 18, 22, 0.6);
    color: var(--text-secondary);
    font-family: "IBM Plex Mono", monospace;
    font-size: 8px;
    white-space: nowrap;
    opacity: 0;
    transform: scale(0.7);
    transition: opacity 0.35s ease, transform 0.35s ease;
}

.fragment-one { top: 0; right: -36px; }
.fragment-two { top: 28px; left: -54px; }
.fragment-three { right: -30px; bottom: 2px; }
.fragment-four { left: -31px; bottom: 5px; }

.ai-waifu-card:hover .memory-orb,
.ai-waifu-card.is-scanning .memory-orb {
    opacity: 1;
    transform: scale(1.08);
}

.ai-waifu-card:hover .memory-fragment,
.ai-waifu-card.is-scanning .memory-fragment {
    opacity: 1;
    transform: scale(1);
}

@keyframes vivian-heartbeat {
    0%, 100% { box-shadow: 0 0 0 0 rgba(158, 230, 194, 0.65); }
    45% { box-shadow: 0 0 0 6px rgba(158, 230, 194, 0); }
}

@keyframes memory-pulse {
    0%, 100% { transform: scale(0.92); filter: brightness(0.9); }
    50% { transform: scale(1.08); filter: brightness(1.2); }
}

@keyframes status-orbit {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}

@media (prefers-reduced-motion: reduce) {
    .vivian-status-dot, .memory-orb-core, .vivian-status-line { animation: none; }
}

.scratch-underlayer-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, #c3b1e1 0%, #a892d6 50%, #8ec8d8 100%);
    color: #0c0c0d;
    font-weight: 800;
    font-size: 15px;
    letter-spacing: 0.05em;
    text-decoration: none;
    border-radius: 12px;
    transition: transform 0.25s ease, box-shadow 0.25s ease, filter 0.25s ease;
    box-sizing: border-box;
    padding: 0 20px;
}

.scratch-underlayer-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 25px rgba(195, 177, 225, 0.45);
    filter: brightness(1.08);
}

.scratch-top-canvas {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    cursor: pointer;
    z-index: 2;
    transition: opacity 0.4s ease;
    border-radius: 12px;
}

@media (max-width: 640px) {
    .scratch-btn-wrapper {
        max-width: 100%;
        height: 44px;
    }
    .scratch-underlayer-btn {
        font-size: 13.5px;
        padding: 0 12px;
        gap: 6px;
    }
}
</style>

<script>
    document.addEventListener('DOMContentLoaded', () => {
      // Wait one frame for CSS layout to settle (fonts, flex sizing)
      requestAnimationFrame(() => {
        const canvas = document.getElementById('githubScratchCanvas') as HTMLCanvasElement | null;
        const wrapper = document.getElementById('githubScratchWrapper');

        if (!canvas || !wrapper) return;

        const ctx = canvas.getContext('2d', { willReadFrequently: true });
        if (!ctx) return;

        let rect = wrapper.getBoundingClientRect();
        let w = (canvas.width = rect.width || 320);
        let h = (canvas.height = rect.height || 46);

        let isRevealed = false;

        // Draw top foil layer showing ORIGINAL button text
        const drawFoilCover = () => {
            if (isRevealed) return;
            ctx.globalCompositeOperation = 'source-over';

            const grad = ctx.createLinearGradient(0, 0, w, h);
            grad.addColorStop(0.0, '#3a3848');
            grad.addColorStop(0.4, '#545068');
            grad.addColorStop(0.7, '#3a3848');
            grad.addColorStop(1.0, '#262432');

            ctx.fillStyle = grad;
            ctx.beginPath();
            ctx.roundRect(0, 0, w, h, 12);
            ctx.fill();

            // Metallic glitter noise
            for (let i = 0; i < 180; i++) {
                ctx.fillStyle = Math.random() > 0.5 ? 'rgba(255,255,255,0.22)' : 'rgba(195,177,225,0.2)';
                ctx.fillRect(Math.random() * w, Math.random() * h, 2, 2);
            }

            // Outer border line
            ctx.strokeStyle = 'rgba(255, 255, 255, 0.18)';
            ctx.lineWidth = 1.5;
            ctx.strokeRect(1, 1, w - 2, h - 2);

            // Responsive font size
            const fontSize = w < 340 ? '12px' : '13.5px';

            // Original text & arrow on cover layer
            ctx.fillStyle = '#ffffff';
            ctx.font = `600 ${fontSize} "IBM Plex Mono", sans-serif`;
            ctx.textAlign = 'center';
            ctx.textBaseline = 'middle';
            ctx.shadowColor = 'rgba(0,0,0,0.6)';
            ctx.shadowBlur = 3;
            ctx.fillText('มาส่องโค้ด / project กันได้น้าา  ➔', w / 2, h / 2);
        };

        drawFoilCover();

        const updateCanvasDimensions = () => {
            if (isRevealed) return;
            rect = wrapper.getBoundingClientRect();
            if (rect.width && rect.height) {
                w = canvas.width = rect.width;
                h = canvas.height = rect.height;
                drawFoilCover();
            }
        };

        window.addEventListener('resize', updateCanvasDimensions, { passive: true });

        const scratchAt = (x: number, y: number) => {
            if (isRevealed) return;

            ctx.globalCompositeOperation = 'destination-out';
            ctx.beginPath();
            ctx.arc(x, y, 22, 0, Math.PI * 2);
            ctx.fill();

            checkRevealed();
        };

        const getCanvasCoords = (e: MouseEvent | TouchEvent) => {
            const r = canvas.getBoundingClientRect();
            let clientX: number, clientY: number;
            if ('touches' in e && e.touches.length > 0) {
                clientX = e.touches[0].clientX;
                clientY = e.touches[0].clientY;
            } else if ('changedTouches' in e && e.changedTouches.length > 0) {
                clientX = e.changedTouches[0].clientX;
                clientY = e.changedTouches[0].clientY;
            } else {
                clientX = (e as MouseEvent).clientX;
                clientY = (e as MouseEvent).clientY;
            }
            return { x: clientX - r.left, y: clientY - r.top };
        };

        const checkRevealed = () => {
            if (isRevealed) return;

            const imgData = ctx.getImageData(0, 0, w, h);
            const pixels = imgData.data;
            let clearCount = 0;

            for (let i = 3; i < pixels.length; i += 16) {
                if (pixels[i] === 0) clearCount++;
            }

            const percent = (clearCount / (pixels.length / 16)) * 100;

            // Must scratch off almost completely (78%+) before clearing remaining foil!
            if (percent > 78) {
                isRevealed = true;
                canvas.style.opacity = '0';
                setTimeout(() => {
                    canvas.style.display = 'none';
                }, 400);
            }
        };

        // Mouse hover scratch
        canvas.addEventListener('mousemove', (e) => {
            const pos = getCanvasCoords(e);
            scratchAt(pos.x, pos.y);
        });

        canvas.addEventListener('mouseenter', (e) => {
            const pos = getCanvasCoords(e);
            scratchAt(pos.x, pos.y);
        });

        // Mobile touch scratch
        canvas.addEventListener('touchstart', (e) => {
            const pos = getCanvasCoords(e);
            scratchAt(pos.x, pos.y);
        }, { passive: true });

        canvas.addEventListener('touchmove', (e) => {
            const pos = getCanvasCoords(e);
            scratchAt(pos.x, pos.y);
        }, { passive: true });

        canvas.addEventListener('click', (e) => {
            const pos = getCanvasCoords(e);
            scratchAt(pos.x, pos.y);
            window.open('https://github.com/celestial-sora', '_blank');
        });
      }); // end requestAnimationFrame
    });
</script>

<script>
    const aiWaifuCard = document.getElementById('aiWaifuCard');
    const vivianStatusText = document.getElementById('vivianStatusText');

    if (aiWaifuCard && vivianStatusText) {
        const statuses = ['Vivian is online', 'Scanning memory orb...', 'Memory system ready'];
        let statusTimer: ReturnType<typeof setInterval> | undefined;
        let statusIndex = 0;

        const startVivianScan = () => {
            aiWaifuCard.classList.add('is-scanning');
            statusIndex = 0;
            vivianStatusText.textContent = statuses[statusIndex];
            statusTimer = setInterval(() => {
                statusIndex = (statusIndex + 1) % statuses.length;
                vivianStatusText.textContent = statuses[statusIndex];
            }, 1200);
        };

        const stopVivianScan = () => {
            aiWaifuCard.classList.remove('is-scanning');
            if (statusTimer) clearInterval(statusTimer);
            statusTimer = undefined;
            vivianStatusText.textContent = statuses[0];
        };

        aiWaifuCard.addEventListener('mouseenter', startVivianScan);
        aiWaifuCard.addEventListener('mouseleave', stopVivianScan);
        aiWaifuCard.addEventListener('click', (event) => {
            if ((event.target as HTMLElement).closest('a')) return;
            aiWaifuCard.classList.toggle('is-scanning');
        });
    }
</script>
